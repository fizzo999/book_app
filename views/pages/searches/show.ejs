<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="./styles/reset.css">
  <link rel="stylesheet" href="./styles/base.css">
  <link rel="stylesheet" href="./styles/layout.css">
  <link rel="stylesheet" href="./styles/modules/module2.css">
  <title>Results from Google Books</title>
</head>
<body id="SearchResultsPage">
  <header>
    <h2>Best Books!</h2>
  </header>
  <div id="searchResultsDiv">
    <h2>Search Results</h2>
    <% for (let i=0; i < bookSearchArray.length; i++) { %>
      <ul>
        <li>
          <img src="<%= bookSearchArray[i].image_url%>" alt="">           
          <h3 class="title"><%= bookSearchArray[i].title %></h3>          
          <h4 class="authors"><%= bookSearchArray[i].authors %></h4>          
          <p class="description"><%= bookSearchArray[i].book_description %></p>          
          <h4 class="pub_date"><%= bookSearchArray[i].pub_date %></h4>        
          <h4 class="isbn"><%= bookSearchArray[i].isbn %></h4>      
      
          <form action="/books" method="POST" >
            <fieldset >
              <input type="hidden" name="title" value="<%= bookSearchArray[i].title %>">
              <input type="hidden" name="author" value="<%= bookSearchArray[i].authors %>">
              <input type="hidden" name="book_description" value="<%= bookSearchArray[i].book_description %>">
              <input type="hidden" name="pubDate" value="<%= bookSearchArray[i].pub_date %>">
              <input type="hidden" name="isbn" value="<%= bookSearchArray[i].isbn %>">
              <input type="hidden" name="image_url" value="<%= bookSearchArray[i].image_url%>">
              <input type="submit" value="Select This Book" class="saveBookBtn">
            </fieldset>
          </form>
        </li>
     </ul>
  </div>
  <%  } %>  
</body>
</html>
